server:
  port: 8080
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/ebnelhaythem
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ebnelhaythem
logstash:
  host: localhost
  port: 5000