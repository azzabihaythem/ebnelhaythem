server:
  port: 8080
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: jdbc:mysql://localhost:3306/ebnelhaythem
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ebnelhaythem
logstash:
  host: localhost
  port: 5000